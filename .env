# .env - PRODUCTION CONFIGURATION (No Localhost)

# Allowed origins - ADD YOUR ACTUAL PRODUCTION DOMAIN
# Remove localhost and add your Vercel deployment domain
ALLOWED_ORIGINS="https://livetvpro.vercel.app"

# For frontend - proxy endpoint
VITE_PROXY_URL="/api/m3u8-proxy"

# Note: In production, the proxy will only accept requests from the allowed origins above
# Make sure to update ALLOWED_ORIGINS when deploying to a new domain
